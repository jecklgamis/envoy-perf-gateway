{{- if eq .Values.configSourceKind "http" }}
{{- if not .Values.gateway.configApiTokenExistingSecret.name }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "envoy-perf-gateway.gateway.fullname" . }}
  labels:
    {{- include "envoy-perf-gateway.gateway.labels" . | nindent 4 }}
type: Opaque
stringData:
  config-api-token: {{ .Values.gateway.configApiToken | quote }}
{{- end }}
{{- end }}
